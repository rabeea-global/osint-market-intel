# Core intelligence stack
yfinance>=0.2.40
feedparser>=6.0.10
requests>=2.31.0

# Charting for the email snapshot (headless Agg backend)
matplotlib>=3.7

# pandas/numpy are pulled by yfinance; listed explicitly for clarity.
# No pandas_ta -> no numba pin, no numpy<2 constraint. Indicators are
# computed natively in analyzer.py (MACD via EMA, RSI via Wilder's RMA).
pandas>=1.5
numpy
